{{-- <!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>403 - Forbidden</title>

    <!-- DaisyUI + Tailwind CDN -->
    <link href="https://cdn.jsdelivr.net/npm/daisyui@3.8.3/dist/full.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>

    <!-- Custom animation -->
    <style>
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .animate-fade-in {
            animation: fadeIn 0.5s ease-out;
        }
    </style>
</head>

<body class="bg-base-200 text-base-content">
    <div class="min-h-screen flex items-center justify-center px-6 py-12">
        <div class="text-center max-w-2xl animate-fade-in">

            <!-- SVG Illustration -->
            <img src="{{ asset('images/access_denied.svg') }}" alt="403 Forbidden"
                class="mx-auto mb-8 w-60 sm:w-72 md:w-80" />

            <!-- Title -->
            <h1 class="text-4xl sm:text-5xl font-bold text-error mb-4">403 - Forbidden</h1>

            <!-- Description -->
            <p class="text-base sm:text-lg mb-6">
                Sorry, you don't have permission to access this page.<br>
                Please contact the administrator if you believe this is a mistake.
            </p>

            <!-- Back Button -->
            <a href="{{ url()->previous() }}" class="btn btn-primary btn-sm">
                ⬅️ Go Back
            </a>
        </div>
    </div>
</body>

</html> --}}
